# Copy to .env and fill in your own values

# Example: GitHub token, registry creds, cluster context, etc.
# GITHUB_TOKEN=
# GHCR_IMAGE_PREFIX=ghcr.io/youruser

# Example: deployment target
# KUBE_CONTEXT=
# KUBE_NAMESPACE=default
